name: Render documents

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  render_document:
    container:
      image: rocker/verse
    steps:
      - name: install
        run: make install/R
